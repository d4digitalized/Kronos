# Audit Kronos — chyby a rychlost (24. 9. 2026)

Výchozí stav: commit `eaeed59`. Opravy z §3 leží v pracovní kopii, **necommitnuté a nenasazené**.
Produkční databázi jsem neprocházel (čtení produkčních dat mi prostředí nepovolilo) — závěry
vycházejí z kódu a migrací. SQL v §6 je ověří na datech.

## 1. Shrnutí

- **Timer (všechny tři stížnosti)** má společný kořen: start/stop dělal prohlížeč ve 3–4 dotazech
  za sebou, **konec záznamu bral z hodin počítače, začátek z hodin serveru**, chyby polykal a
  chybu načtení vykládal jako „nic neběží". Opraveno — start/stop jsou teď jedna funkce v DB.
- **„Neukládají se položky"** jsou z velké části záznamy, které uložené jsou, ale nejsou vidět:
  Můj čas se po Stopu neobnovil a Přehledy nad 1000 záznamů za období potichu ořezávaly. Obojí
  opraveno. Skutečná ztráta času hrozí při **smazání karty nebo projektu** (kaskáda smaže čas
  všech lidí) — to čeká na tvé rozhodnutí (§5.1).
- **„Výpadky"**: aplikace neměla žádné chybové stránky, takže po každém nasazení (push do `main`)
  stačilo v otevřené záložce otevřít kartu a spadla celá na „Application error". Krátká chyba
  spojení navíc vyhazovala na přihlášení nebo ukazovala 404. Opraveno.
- **Rychlost**: nejdražší jsou RLS politiky — pomocné funkce se volají pro každý řádek
  (u člena ~10 volání na úkol). Dále chybějící indexy (opraveno v 0045) a obrazovky, které
  stahují celé tabulky (nástěnka všechny hotové karty, Task force všechny úkoly firmy).
- **Bezpečnost**: kritická díra — admin libovolné firmy mohl přes `setMemberFlag` přepsat
  libovolný sloupec členství (i `role` nebo `workspace_id` = přesun do cizí firmy). Opraveno,
  stejně jako open redirect v přihlášení. Další nálezy v §5.4.

## 2. Stížnosti uživatelů → příčiny

### 2.1 „Zasekává se počítadlo"

1. **Dvoje hodiny.** `started_at` zapisuje databáze (`default now()`), běžící čas se ale počítal
   z `Date.now()` prohlížeče a záporné hodnoty se ořízly na nulu
   ([format.ts](../src/lib/format.ts)). Počítač s hodinami o N sekund pozadu ukazoval po startu
   N sekund 0:00:00 — a když dorazil skutečný záznam, skočil čas zpátky na nulu a zase stál.
2. **Chyba = „nic neběží".** Lišta přenačítá stav při každém návratu do okna/záložky a
   **jakoukoli chybu** vyložila jako „timer neběží" (šedé 0:00:00). Typicky po probuzení z
   uspání (síť ještě nenaskočila), při výpadku Wi-Fi nebo po vypršení přihlášení — supabase-js
   pak pošle dotaz anonymně a RLS vrátí prázdno bez chyby. Na iPadu se focus mode zavřel nebo
   ukázal „pauza" se stojícím časem a tlačítka nereagovala. Timer přitom na serveru běžel dál.
3. **Dotazy bez timeoutu.** Zaseklé spojení po uspání drželo tlačítka zamčená.

### 2.2 „Musím dvakrát kliknout na Stop"

1. **Kontrola v DB.** Tabulka má `check (stopped_at > started_at)`. S opožděnými hodinami
   počítače databáze Stop odmítla („Timer se nepodařilo zastavit"), timer se vrátil a klikat se
   muselo znovu, dokud neuběhlo víc času, než je rozdíl hodin.
2. **Tichý neúspěch.** Chyba při zjišťování běžícího timeru se ignorovala — Stop neudělal nic,
   bez hlášky, timer běžel dál.
3. **Souběh s přenačtením.** Klik z jiné aplikace přímo na Stop nejdřív vyvolá focus okna a s
   ním přenačtení; jeho odpověď mohla dorazit až po zastavení a vrátit „běží".
4. **Zablokované tlačítko.** Během rozběhu startu (4 dotazy za sebou) byl Stop `disabled` —
   klik propadl.

### 2.3 „Neukládají se položky z timeru"

1. **Můj čas / Report se po Stopu neobnovil** — nový záznam nebyl vidět, dokud člověk neodešel
   a nevrátil se. Pro lidi v režimu „jen měření času" je to jejich hlavní obrazovka.
2. **Přehledy ořezané na 1000 řádků.** Dotaz bez řazení a stránkování; Supabase vrátí nejvýš
   1000 řádků a zbytek potichu zahodí — náhodné záznamy „chybí", součty nesedí s výkazem.
   Totéž výkaz k tisku za delší období.
3. **Přepnutí timeru na jinou kartu.** Když selhalo tiché zastavení předchozího, vložení nového
   narazilo na unikátní index běžícího timeru a chyba se spolkla — čas běžel dál na starém úkolu.
4. **Nedopsaný popis** se ukládal jen při opuštění pole. Na iPadu klepnutí na Stop pole
   neopustí — popis zmizel.
5. **Timer z jiné firmy.** Lišta ho ukazovala bez upozornění, Můj čas ale filtruje firmu →
   „záznam tam není". Změnou projektu v liště šel navíc záznamu přiřadit projekt cizí firmy.
6. **Smazání karty nebo projektu smaže i odpracovaný čas všech lidí** (`on delete cascade`
   v [0001](../supabase/migrations/0001_init.sql) a [0002](../supabase/migrations/0002_kanban.sql)).
   Dialog to říká jen tomu, kdo maže. **Neopraveno** — viz §5.1.

### 2.4 „Výpadky"

1. **Žádné chybové stránky** (`error.tsx`, `global-error.tsx`). Jakákoli chyba vykreslení
   shodila celou aplikaci. Nejčastější spouštěč je nasazení: otevřená záložka po deployi
   nenajde starý chunk karty (`dynamic(() => import(...CardModal))` v 7 obrazovkách) nebo
   dialogu „+" → pád. Při několika deployích denně to lidé zažívají jako výpadek. Stejně
   končí volání server action ze staré záložky.
2. **Síťová chyba = odhlášení.** Proxy chybu z `getClaims()` ignorovala — chvilková
   nedostupnost Supabase Auth (studený start, síť) přesměrovala na `/login`. Chyba dotazů
   kontextu firmy udělala 404, chyba na úvodní stránce hlášku „Zatím nejsi členem žádného
   workspace".
3. **Chyby dotazů se zobrazují jako prázdná data** — „Nemáš žádné úkoly", prázdná nástěnka,
   karty v panelu „bez sloupce"; prázdný výsledek se navíc uloží do cache. Čtecí dotazy
   supabase-js sám opakuje (1+2+4 s), takže uživatel vidí ~7 s skeleton a pak prázdno.
   **Neopraveno plošně** (§5.1) — opraveno jen u timeru, Můj čas, Přehledů, výkazu a poznámek.
4. **Ztráta dat po chybě:** poznámky po chybě načtení přepsal autosave tím, co člověk napsal do
   prázdného pole; % výkaz nejdřív smazal den a pak vkládal — při chybě den zmizel. Opraveno.

## 3. Co je opravené (pracovní kopie, necommitnuté)

| Soubor | Změna |
| --- | --- |
| [0044_timer_na_serveru.sql](../supabase/migrations/0044_timer_na_serveru.sql) | `timer_current` / `timer_start` / `timer_stop`: jedna transakce, čas DB, zámek na uživatele (dvě záložky, MCP), `security invoker` — RLS beze změny |
| [0045_indexy.sql](../supabase/migrations/0045_indexy.sql) | 6 chybějících indexů (úkoly podle projektu a autora, kaskády, členství podle uživatele, nepřečtené notifikace) |
| [lib/timer.ts](../src/lib/timer.ts) | volá funkce v DB; chyba se hlásí a stav se přenačte; timeout 15 s; jeden start naráz (dvojklik na ▶ u karty); dokud 0044 neběží, jede stará cesta s ošetřenými chybami |
| [lib/format.ts](../src/lib/format.ts) | běžící čas podle hodin serveru (srovnání z každé odpovědi timeru) |
| [TimerBar.tsx](../src/components/TimerBar.tsx) | chyba ≠ „nic neběží" (žlutá tečka + opakování 5/15/30/60 s, i po `online`); starší odpověď nepřepíše novější; Stop během startu se zařadí; ochrana proti dvojkliku (▶/■ jsou na stejném místě); nedopsaný popis jde se zastavením; změny během startu se nezahodí; „běží ve firmě X" |
| [ElapsedClock.tsx](../src/components/ElapsedClock.tsx) | každou sekundu se překreslí jen čas, ne celá lišta |
| [FocusMode.tsx](../src/components/FocusMode.tsx) | pauza / stop / zavření předávají rozepsanou poznámku |
| [MyTimeView.tsx](../src/components/MyTimeView.tsx) | obnoví se po každé změně timeru; chyba ≠ „žádné záznamy"; úprava/smazání běžícího záznamu se propíše do lišty; výchozí datum místní (bylo UTC) |
| [Toaster.tsx](../src/components/Toaster.tsx) | hlášky nad focus módem (dřív pod ním nebyly vidět) |
| [ReportsView.tsx](../src/components/ReportsView.tsx), [VykazView.tsx](../src/components/VykazView.tsx), [lib/fetchAll.ts](../src/lib/fetchAll.ts) | stránkování přes limit 1000, chyba ≠ nulové součty, u Přehledů pořadí odpovědí |
| [app/error.tsx](../src/app/error.tsx), [app/w/[wsId]/error.tsx](../src/app/w/%5BwsId%5D/error.tsx), [app/global-error.tsx](../src/app/global-error.tsx), [ErrorFallback.tsx](../src/components/ErrorFallback.tsx) | chybové stránky; po nasazení nové verze se stránka jednou sama obnoví; v rámci firmy zůstane menu i lišta s timerem |
| [proxy.ts](../src/proxy.ts), [lib/session.ts](../src/lib/session.ts), [lib/jwks.ts](../src/lib/jwks.ts), [app/page.tsx](../src/app/page.tsx) | síťová chyba ≠ odhlášení / 404 / „nejsi členem" → „Zkusit znovu"; proxy používá i statické JWKS |
| [NotesView.tsx](../src/components/NotesView.tsx) | po chybě načtení se needituje (žádný přepis poznámek) |
| [PercentReportView.tsx](../src/components/PercentReportView.tsx) | nejdřív vložit nové bloky, pak smazat staré podle id |
| [actions/members.ts](../src/app/actions/members.ts) | `setMemberFlag` jen s povolenými sloupci a boolean hodnotou |
| [login/page.tsx](../src/app/login/page.tsx), [auth/callback/route.ts](../src/app/auth/callback/route.ts), [lib/safeNext.ts](../src/lib/safeNext.ts) | open redirect (`/\evil.com`, `/<TAB>/evil.com` prošly dosavadní kontrolou) |

**Ověřeno:** `tsc`, `eslint`, `next build`, kouřový test produkčního buildu (nepřihlášený →
307 na `/login`, bypassy open redirectu → `/`). **Neověřeno:** nic za přihlášením — nemám
testovací účet. Postup ručního testu je v §7.

## 4. Nasazení oprav

1. **Supabase → SQL Editor:** spustit `0044_timer_na_serveru.sql`, pak `0045_indexy.sql`. Obě
   jen přidávají (funkce, indexy), dnešní verze aplikace s nimi funguje dál. Kdyby šel kód ven
   dřív, timer pojede starou (už ošetřenou) cestou, dokud 0044 neproběhne.
2. `npm run dev` a projít checklist v §7 (lokální dev míří na produkční Supabase — testovací
   záznamy pak smaž v Můj čas).
3. Commit + push do `main`.
4. **Vercel → Settings → Advanced → Skew Protection** (pokud je projekt na Pro) — otevřené
   záložky pak po deployi dostanou soubory své verze. Jinak aspoň nasazovat mimo pracovní dobu.
5. Spustit SQL z §6 a podívat se do **Supabase → Logs**: Postgres `time_entries_check` a
   `one_running_timer_per_user` (potvrdí §2.2/1 a §2.3/3), API 401 „JWT expired", Auth
   `refresh_token_already_used`.

## 5. Další nálezy — k řešení

### 5.1 Priorita 1

- **Mazání odpracovaného času kaskádou.** Návrh: čas zůstane, jen bez vazby na kartu/projekt
  (+ upravit text dialogu v [CardModal.tsx:629](../src/components/CardModal.tsx#L629)):

  ```sql
  alter table public.time_entries
    drop constraint time_entries_task_id_fkey,
    add constraint time_entries_task_id_fkey foreign key (task_id)
      references public.tasks (id) on delete set null,
    drop constraint time_entries_project_id_fkey,
    add constraint time_entries_project_id_fkey foreign key (project_id)
      references public.projects (id) on delete set null;
  ```

  Mění chování (záznam po smazané kartě zůstane s popisem, ale bez názvu karty) — ideálně
  doplnit trigger, který před smazáním úkolu zkopíruje jeho název do prázdných popisů.
- **Chyby dotazů v ostatních obrazovkách** (§2.4/3): sdílený helper, který při chybě nechá
  poslední data, nic neuloží do cache a ukáže „Nepodařilo se načíst — Zkusit znovu".
  Týká se [BoardView.tsx:219](../src/components/BoardView.tsx#L219) (+ `cacheSet` na ř. 330),
  TasksView, MyTasksView, InboxView, PriorityListView, DelegatedView, MyDayView, BoardsList,
  InboxCount, NotificationSettings.
- **Výkon RLS** (§5.2, první bod) — největší páka na rychlost; návrh přepisu mám připravený,
  potřebuje ověřit na kopii dat.

### 5.2 Rychlost

- **RLS volá funkce pro každý řádek.** `is_ws_admin(workspace_id)`, `is_project_member(project_id)`,
  `is_task_mine(id)` jsou `security definer` s argumentem z řádku → volají se pro každý řádek
  (a `is_ws_admin` uvnitř volá ještě `is_super_admin`). `auth.uid()` není nikde zabalené jako
  `(select auth.uid())` (152×). Úkol pro člena stojí ~10 volání, a stejná politika se platí
  znovu u každé vnořené tabulky (`task_assignees`, `task_labels`… mají `exists (select … from tasks)`).
  Řešení: množinové helpery (`my_ws_ids()`, `my_admin_ws_ids()`, `my_project_ids()`) volané
  jako `(select …)` — vyhodnotí se jednou na dotaz. Přístupová práva zůstanou stejná.
- **Nástěnka** stahuje všechny hotové karty projektu a štítky celé firmy a po většině změn
  pouští znovu všech 10 dotazů ([BoardView.tsx:171-217](../src/components/BoardView.tsx#L171)).
  Hotové karty stránkovat (`order completed_at desc limit 50`), štítky omezit na projekt.
- **Task force** stahuje všechny úkoly firmy včetně hotových a všechny řešitele firmy
  ([TasksView.tsx:86-117](../src/components/TasksView.tsx#L86)) — pomalé a navíc naráží na limit 1000.
- **Počítadlo Inboxu** běží 2–3× (Sidebar i MobileNav,
  [Sidebar.tsx:145](../src/components/Sidebar.tsx#L145),
  [MobileNav.tsx:129](../src/components/MobileNav.tsx#L129)) a pokaždé 2 dotazy při každé změně
  úkolů. Inbox „Utříděno" = 9 dotazů, zavření karty 14 ([InboxView.tsx:441-445](../src/components/InboxView.tsx#L441)).
- **Karta** = 15 dotazů při otevření, checklisty sekvenčně
  ([CardChecklists.tsx:26-48](../src/components/CardChecklists.tsx#L26) — stačí embed `checklist_items(*)`).
- **Notifikace** se nikdy nemažou; počet nepřečtených dosud četl celou historii (index v 0045).
- **Supabase tarif:** README počítá s free tierem (sdílené CPU). Při ~20 souběžných
  dotazech na načtení nástěnky a drahém RLS stojí za to zkontrolovat Database → Reports (CPU)
  a Query Performance.

### 5.3 Spolehlivost a správnost dat

- **Esc v kartě zahodí rozepsané** (název/popis/komentář); Esc v pickeru nebo v @zmínkách zavře
  celou kartu ([CardModal.tsx:197-204](../src/components/CardModal.tsx#L197),
  [NewTaskDialog.tsx:68-75](../src/components/NewTaskDialog.tsx#L68)).
- **Autosave karty při chybě tiše zahodí změnu** — `savedRef` se nastaví před uložením,
  termín/priorita zůstanou v poli i po neúspěchu ([CardModal.tsx:211](../src/components/CardModal.tsx#L211), 552, 559).
- **Starší odpověď přepíše novější** (chybí pořadí dotazů): Můj den a % výkaz při přepínání
  týdnů, Projekty → členové ([ProjectsView.tsx:279-317](../src/components/ProjectsView.tsx#L279) —
  pak se přidávají/odebírají členové podle špatného projektu), Inbox, dialog Nový úkol.
- **Dvojí odeslání Enterem** založí dva řádky: sloupec, karta, podúkol, checklist, projekt,
  kontakt, firma ([BoardView.tsx:370](../src/components/BoardView.tsx#L370), 422 a další).
- **% výkaz:** šipky týdne nemění vybraný den → uložení přepíše den mimo zobrazený týden
  ([PercentReportView.tsx:222](../src/components/PercentReportView.tsx#L222), 267).
- **Priority list** otevírá kartu s členy aktuální firmy i u úkolů jiné firmy
  ([PriorityListView.tsx:87-92](../src/components/PriorityListView.tsx#L87)).
- **Lead, který není členem projektu, nemůže úkol upravit ani dokončit** — `with check`
  v `tasks_update` ([0040](../supabase/migrations/0040_oprava_insert_returning.sql)) ověřuje
  projekt a sloupec přes RLS, které leada nepustí.
- **Opakované úkoly:** dokonči → otevři → dokonči = duplicitní kopie (0023, trigger recurrence).
- **Mazání zakázané RLS hlásí úspěch** ([CardModal.tsx:632](../src/components/CardModal.tsx#L632),
  CardAttachments.tsx:100) — PostgREST vrátí 204 s nulou smazaných řádků.
- **UTC datum místo místního** mezi 0:00 a 2:00 ([DelegatedView.tsx:199](../src/components/DelegatedView.tsx#L199),
  [BoardCard.tsx:44](../src/components/BoardCard.tsx#L44)).
- **Server actions bez try/catch** → po deployi zaseknutý skeleton Členů nebo tlačítko
  ([MembersView.tsx:57-71](../src/components/MembersView.tsx#L57), ApiTokens.tsx:46).
- **Externí volání bez timeoutu:** Google kalendář (server action, na kterou karta čeká), Resend,
  drain notifikací; `maxDuration` má jen MCP.
- **Inbound webhook** může založit duplicitní komentář — odpovídá až po odeslání e-mailů a Svix
  při pomalé odpovědi doručuje znovu ([api/inbound/route.ts:110-121](../src/app/api/inbound/route.ts#L110)).
- **MCP `start_timer` / `stop_timer`** ignorují chyby a míchají čas Vercelu s časem DB
  ([mcp/tools.ts:1174-1239](../src/lib/mcp/tools.ts#L1174)) → přepnout na funkce `timer_*` z 0044.
- **Chybí logování chyb serveru** (`instrumentation.ts` s `onRequestError`, případně Sentry) —
  výpadky dnes nejsou nikde vidět.

### 5.4 Bezpečnost

- ~~`setMemberFlag` přepíše libovolný sloupec service-role klíčem~~ — opraveno.
- ~~Open redirect v přihlášení~~ — opraveno.
- **OAuth pro MCP:** klienta zaregistruje kdokoli s libovolným redirect URI a jménem a souhlas
  ukazuje jen to jméno ([oauth/register](../src/app/api/oauth/register/route.ts),
  [oauth/authorize/page.tsx:73](../src/app/oauth/authorize/page.tsx#L73)) — podvržený „Claude"
  po kliknutí na Povolit získá přístup k úkolům. Ukázat doménu redirectu, allow-list
  (claude.ai, localhost), `http:` jen pro localhost.
- **Kalendář:** admin nastaví členovi `notify_email` na adresu jiného člověka z domény a service
  account pak zapisuje do jeho Google kalendáře ([calendarSync.ts:17-25](../src/lib/calendarSync.ts#L17)).
- **`listAddablePortalUsers`:** admin kterékoli firmy vidí jména a e-maily všech uživatelů
  portálu ([members.ts:31](../src/app/actions/members.ts#L31)).
- **`CRON_SECRET`:** když proměnná chybí, projde hlavička `Bearer undefined` a reply tokeny se
  podepisují prázdným klíčem ([cron/digest/route.ts:31](../src/app/api/cron/digest/route.ts#L31),
  [email.ts:56](../src/lib/email.ts#L56)).
- **OAuth access tokeny** jsou podepsané Supabase JWT secretem bez `aud` — po zrušení legacy
  secretu v Supabase přestane MCP fungovat.
- **`/auth/confirm`** spotřebuje jednorázový token už na GET — antivirové skenery odkazů
  v e-mailech „spálí" pozvánky a obnovy hesla.

## 6. Ověření na datech (jen čtení)

Spustit po blocích v Supabase SQL Editoru. Záznam spuštěný timerem poznáme podle
`started_at = created_at` (obojí `now()` při vložení).

```sql
-- 1) Co právě běží a jak dlouho (zapomenuté / „nezastavitelné" timery)
select left(user_id::text, 8) as uzivatel, started_at, now() - started_at as bezi
from public.time_entries where stopped_at is null order by started_at;

-- 2) Délky záznamů z timeru: hodně „< 5 s" = dvojkliky, hodně „> 12 h" = nezastavené timery
with t as (
  select extract(epoch from stopped_at - started_at) as s from public.time_entries
  where stopped_at is not null and abs(extract(epoch from started_at - created_at)) < 1
)
select case when s < 5 then '1: < 5 s' when s < 60 then '2: 5–60 s'
            when s < 300 then '3: 1–5 min' when s < 3600 then '4: 5 min – 1 h'
            when s < 4*3600 then '5: 1–4 h' when s < 8*3600 then '6: 4–8 h'
            when s < 12*3600 then '7: 8–12 h' else '8: > 12 h' end as delka,
       count(*) as pocet
from t group by 1 order by 1;

-- 3) Rozjeté hodiny v počítačích: při přepnutí timeru se předchozí záznam zastavil časem
--    PROHLÍŽEČE a nový začal časem SERVERU o zlomek sekundy později.
--    Mezera 0,1–0,5 s = hodiny v pořádku; záporná / překryvy = předbíhají;
--    minimum trvale v desítkách sekund = opožďují se (a pak padal Stop).
with t as (
  select user_id, started_at, stopped_at,
         lead(created_at) over (partition by user_id order by started_at) as next_created,
         lead(started_at) over (partition by user_id order by started_at) as next_started
  from public.time_entries where abs(extract(epoch from started_at - created_at)) < 1
)
select left(user_id::text, 8) as uzivatel, count(*) as rychla_prepnuti,
       count(*) filter (where next_started < stopped_at) as prekryvy,
       round(min(extract(epoch from next_created - stopped_at))::numeric, 2) as min_mezera_s,
       round((percentile_cont(0.5) within group (
         order by extract(epoch from next_created - stopped_at)))::numeric, 2) as median_mezera_s
from t
where stopped_at is not null and next_created is not null
  and extract(epoch from next_created - stopped_at) between -3600 and 10
group by user_id order by prekryvy desc, min_mezera_s;

-- 4) Krátké záznamy, po kterých do 30 s následoval další (otisk „klik nic neudělal → znovu")
with t as (
  select user_id, started_at, stopped_at,
         lead(started_at) over (partition by user_id order by started_at) as next_started
  from public.time_entries where abs(extract(epoch from started_at - created_at)) < 1
)
select left(user_id::text, 8) as uzivatel, count(*) as kratke_s_navazujicim
from t
where stopped_at is not null and stopped_at - started_at < interval '5 seconds'
  and next_started - stopped_at < interval '30 seconds'
group by user_id order by 2 desc;

-- 5) Záznamy s projektem z JINÉ firmy (změna projektu v liště u timeru z jiné firmy)
select count(*) as projekt_z_jine_firmy
from public.time_entries e join public.projects p on p.id = e.project_id
where p.workspace_id <> e.workspace_id;
```

## 7. Ruční test oprav timeru

Po spuštění 0044 a `npm run dev`:

1. ▶ v liště → čas běží hned od 0:00:00; ■ → stačí jeden klik, hláška „Záznam uložen (…)",
   na Můj čas se záznam objeví bez přechodu jinam.
2. Dvojklik na ▶ i na ■ → nevznikne druhý záznam ani nový timer.
3. ▶ na kartě a hned ■ → zastaví se právě spuštěný záznam.
4. Rozepsat popis v liště a hned ■ (bez Enteru) → popis je v záznamu.
5. Focus mode: pauza, pokračovat, stop; poznámka dopsaná bez Enteru se uloží; chyba je vidět nad overlayem.
6. DevTools → Network → Offline při běžícím timeru → lišta dál ukazuje běžící čas a žlutou tečku;
   po zapnutí sítě tečka zmizí. ■ offline → hláška o chybě a timer zůstane běžet.
7. Hodiny počítače o 2 minuty dozadu → start ukazuje správný čas, Stop jde napoprvé.
8. Dvě záložky: start v jedné, druhá po přepnutí ukáže správný stav.
9. Timer spuštěný ve firmě A, přepnout do firmy B → lišta „běží ve firmě A", bez výběru projektu.
10. Přehledy za celý rok (admin) → součty sedí s výkazy jednotlivých lidí.
